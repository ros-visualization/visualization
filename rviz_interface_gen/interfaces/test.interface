interface Test
{
  triple(uint32 val) returns (uint32 val);
  blockFor(duration d);
  async noBlock();
  
  complex(std_msgs/String name, float32 val) returns (std_msgs/String ret);
  
  multipleReturn(uint32 val) returns (uint32 val, uint32 val_doubled);
  withString(string str);
  
  fastcall fastCall(uint32 foo) returns (uint32 bar);
  async fastcall fastCallAsync(uint32 foo) returns (uint32 bar);
}

message Foo
{
  uint32 bar;
}
